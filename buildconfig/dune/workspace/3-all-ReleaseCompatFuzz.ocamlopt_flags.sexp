(-O3); Used in dune.env.workspace.inc. See https://dune.readthedocs.io/en/stable/concepts.html#ordered-set-language

; Flags
; -----
; -O3 == Release builds should use the highest level of optimization.
;        Sometimes optimizations make the code perform worse. Use performance testing to see which optimization level performs best..
